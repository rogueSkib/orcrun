var BG_WIDTH = G_BG_WIDTH;
var BG_HEIGHT = G_BG_HEIGHT;

exports = {
	"space": [
		{
			id: "hole",
			needsView: false,
			width: BG_WIDTH / 8,
			gapRange: [0.8 * BG_WIDTH, 1.2 * BG_WIDTH]
		}
	]
};
